def is_palindrome(s):
    return s == s[::-1]
total = 0
for n in range(1, 1_000_000):
    decimal = str(n)
    binary = bin(n)[2:]
    if is_palindrome(decimal) and is_palindrome(binary):
        total += n
print(total)
